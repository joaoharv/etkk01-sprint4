# Dependencias da camada ETL (src/) e dos testes.
#
# Sem versoes fixas de proposito: a imagem Docker do Airflow instala esta lista
# com o constraint oficial do Airflow 2.9.3 / Python 3.11, que ja resolve as
# versoes compativeis (pandas 2.1.4, openpyxl 3.1.5, SQLAlchemy 1.4.52,
# psycopg2-binary 2.9.9, etc). Fixar aqui so criaria conflito com esse constraint.
#
# Uso local (fora do Docker):
#   pip install -r requirements.txt \
#     -c https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.11.txt

pandas
openpyxl
SQLAlchemy
psycopg2-binary
python-dotenv          # carrega o .env quando o ETL roda fora dos containers

# ML (Plano 2.1) — nao fixadas de proposito, mesmo racional do resto do arquivo.
# O constraint do Airflow 2.9.3/Python 3.11 fixa numpy==1.26.4, mas nao pina
# scikit-learn/scipy/joblib (o Airflow nao depende deles) — pip resolve a
# versao mais recente compativel com esse numpy sozinho.
scikit-learn
joblib

# Testes de integracao (Fase 9)
pytest
