# ----- Ambiente / segredos -----
.env
*.env
!.env.example

# ----- Python -----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# ----- Airflow (runtime local) -----
logs/
airflow.db
airflow.cfg
standalone_admin_password.txt

# ----- Docker -----
# volumes nomeados ficam no Docker; nada de bind local aqui

# ----- IDE / SO -----
.vscode/
.idea/
.DS_Store
Thumbs.db

# ----- Saidas de analise -----
*.log
output/
docs/

# ----- Claude Code (skills locais, nao fazem parte do produto) -----
/.claude/skills/

# ----- Model Registry (Plano 2.1) -----
# .pkl e binario, gerado por src/ml/train_*.py, atado a versao de sklearn/numpy do
# container -- nao versionado. current.json/metadata.json sao a trilha de auditoria
# (pequenos, texto) e continuam versionados normalmente.
/models/**/*.pkl